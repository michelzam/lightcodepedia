{%- comment -%}
Back-to-top button removed: it floated at the bottom-right and overlapped the
✏️ Edit FAB. Browsers already provide native scroll-to-top, so this include now
renders nothing. Kept as a file so existing {% include backtotop.md %} calls
remain valid.
{%- endcomment -%}
